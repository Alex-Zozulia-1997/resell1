import PageWrapper from '@/components/wrapper/page-wrapper';

export default function ResidentialProxiesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <PageWrapper>{children}</PageWrapper>;
}
